# v0.11.1 Repositioning PR Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Realign every Tierkit copy/IA/CLI-help surface from "local-first hybrid plugin runtime" to "local-first cost optimizer and token firewall for AI coding CLIs," with zero runtime behavior change.

**Architecture:** Strictly a docs/copy/UI-IA realignment. The only source files modified are: package.json description fields (root + 9 workspaces), `packages/vscode-tierkit/package.nls.json`, README.md, docs/SPEC.md, docs/INTEGRATIONS.md, `packages/core/src/runtime/ui/gui.ts` (UI strings + i18n + card grouping/order — **no new fetches, no new endpoints, no filesystem scans**), and the `Command.Usage({ category: ... })` blocks of CLI commands. No code logic changes. No new tests. Existing 635 tests + typecheck must stay green.

**Tech Stack:** Markdown / JSON / TypeScript (string-only edits to `gui.ts` and CLI `Command.Usage` blocks).

**Spec:** [`docs/superpowers/specs/2026-05-19-product-repositioning-design.md`](../specs/2026-05-19-product-repositioning-design.md)

**Pre-flight findings (already verified by plan author, 2026-05-19):**
- No vitest `__snapshots__` directory exists anywhere in the repo.
- No test asserts the old copy strings (`Mission Control`, `Local-first hybrid`, `hybrid plugin runtime`). One comment at `packages/core/test/runtimeServerSessionAndUi.test.ts:153` uses "Mission Control UI" but it's a code comment, not an assertion.
- `tierkit connect` supports `roo | cline | continue` only — confirmed at `packages/cli/src/commands/ConnectCommand.ts:5` and `packages/core/src/usecases/connectTool.ts:29`.

---

## Task 1: Root + 9 workspace `package.json` descriptions + VS Code NLS

**Files:**
- Modify: `package.json` (root)
- Modify: `packages/core/package.json`
- Modify: `packages/cli/package.json`
- Modify: `packages/agent/package.json`
- Modify: `packages/adapter-roo/package.json`
- Modify: `packages/adapter-cline/package.json`
- Modify: `packages/adapter-continue/package.json`
- Modify: `packages/client/package.json`
- Modify: `packages/plugin-superpowers/package.json`
- Modify: `packages/vscode-tierkit/package.nls.json`

- [ ] **Step 1: Verify current values**

Run:
```sh
grep -H '"description"' package.json packages/*/package.json packages/vscode-tierkit/package.nls.json
```
Expected: 11 lines, all leading with "Local-first hybrid plugin runtime" / "Tierkit core: plugin format" / etc. (the old positioning).

- [ ] **Step 2: Replace root `package.json` description**

In `package.json`, change:
```jsonc
"description": "Local-first hybrid plugin runtime for AI coding agents",
```
to:
```jsonc
"description": "Local-first cost optimizer and token firewall for AI coding CLIs.",
```

- [ ] **Step 3: Replace `packages/core/package.json` description**

Change:
```jsonc
"description": "Tierkit core: plugin format, validator, loader, model profiles, adapter interface, usecases. Tool- and CLI-framework-agnostic.",
```
to:
```jsonc
"description": "Tierkit core — context compression, secret redaction, cost-aware model routing, quality comparison, plugin runtime. Tool- and CLI-framework-agnostic.",
```

- [ ] **Step 4: Replace `packages/cli/package.json` description**

Change:
```jsonc
"description": "Tierkit command-line interface — thin clipanion layer over @tierkit/core usecases.",
```
to:
```jsonc
"description": "The `tierkit` CLI — measure token savings, build compressed contexts, route work across local / private / cloud models, and audit cost.",
```

- [ ] **Step 5: Replace `packages/agent/package.json` description**

Change:
```jsonc
"description": "Tierkit's built-in coding agent. Cline-style XML tool calling with Tierkit policy gates (routing, security, plugin rules) natively integrated.",
```
to:
```jsonc
"description": "Tierkit's built-in coding agent — optional local-first executor that benefits from Tierkit's cost layer (compression / routing / quality / budget) without needing an external CLI.",
```

- [ ] **Step 6: Replace adapter descriptions**

`packages/adapter-roo/package.json`:
```jsonc
"description": "Tierkit integration for Roo Code / Zoo Code. Routes Roo's model calls through Tierkit's cost layer or exports policy as `.roomodes` + `.roo/`.",
```

`packages/adapter-cline/package.json`:
```jsonc
"description": "Tierkit integration for Cline. Routes Cline's model calls through Tierkit's cost layer or exports policy as `.clinerules/`.",
```

`packages/adapter-continue/package.json`:
```jsonc
"description": "Tierkit integration for Continue. Routes Continue's model calls through Tierkit's cost layer or exports policy as `.continue/{config.yaml, rules, prompts}`.",
```

- [ ] **Step 7: Replace `packages/client/package.json` description**

```jsonc
"description": "Typed HTTP client for the Tierkit cost-layer daemon. Used by VS Code extensions, web UIs, and any tool integrating with a running `tierkit runtime`.",
```

- [ ] **Step 8: Replace `packages/plugin-superpowers/package.json` description**

```jsonc
"description": "Bundled Tierkit policy rule packs (currently named `superpowers-{free,guided,balanced,strict}` — slug rename deferred to v0.13). Apply per-task discipline and tool permissions on top of cost routing.",
```

- [ ] **Step 9: Replace `packages/vscode-tierkit/package.nls.json` description**

Change:
```jsonc
"description": "Local-first hybrid model routing toolkit. The Tierkit runtime starts automatically; the sidebar lets you run tasks, drive workflow sessions, classify commands, and watch usage.",
```
to:
```jsonc
"description": "Local-first cost optimizer for AI coding CLIs — runs the Tierkit daemon and shows real-time token savings, compressed-vs-baseline quality, and cost routing in a VS Code sidebar.",
```

- [ ] **Step 10: Verify and typecheck**

Run:
```sh
grep -H '"description"' package.json packages/*/package.json packages/vscode-tierkit/package.nls.json | grep -E "cost optimizer|cost layer|cost-layer|context compression|policy rule packs"
```
Expected: 11 matching lines (one per file just edited).

Run:
```sh
pnpm -r typecheck
```
Expected: clean. (Descriptions don't affect typecheck, but run for sanity.)

- [ ] **Step 11: Commit**

```sh
git add package.json packages/*/package.json packages/vscode-tierkit/package.nls.json
git commit -m "docs(v0.11.1): repositioning — package descriptions

Root + 9 workspace package.json + VS Code marketplace NLS now describe
Tierkit as a cost optimizer / token firewall for AI coding CLIs.
String-only edits. No behavior change.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

---

## Task 2: `README.md` §1 rewrite (EN + KR intro blocks)

**Files:**
- Modify: `README.md` (lines 1–14 ish — title, lead paragraph, status, Korean intro block)

- [ ] **Step 1: Read current state to confirm boundaries**

Run:
```sh
sed -n '1,14p' README.md
```
Expected: Title `# Tierkit`, then two intro paragraphs (lead + plugin format), then the long `> Status: **v0.11.0 (v1.7-spike).** ...` paragraph, then the `> 🇰🇷 **한글 안내**` block.

- [ ] **Step 2: Replace the title + intro + status block with the new lead**

Open `README.md`. Replace lines 1 through 7 (inclusive — title, two intro paragraphs, blank, status block) with:

```markdown
# Tierkit

Tierkit is a **local-first cost optimizer and token firewall for AI coding CLIs**.

It helps tools like Claude Code, Codex CLI, Gemini CLI, Roo Code, Cline,
Continue, and aider spend fewer cloud tokens — without losing task quality.
Before any premium cloud model sees your project, Tierkit can search files
locally, compress them into a minimal high-signal prompt, redact secrets,
compare compressed vs. baseline output side-by-side with real provider
usage, and route easy work to your local or private models so the expensive
model is only used when it actually has to be.

> Status: **v0.11.0**. Foundation shipped — `tierkit context build / show /
> send / compare` lets you measure token savings + response quality on real
> tasks today. v0.11.1 is a docs/UI realignment around this product
> direction. v0.12 adds local-model preprocessing on top of the
> deterministic compressor. v0.13 wires the gateway into Codex / Roo /
> Cline / Continue / aider, and an MCP server for Claude Code and Gemini
> CLI. Past releases (v0.10.x) added the routing engine, response-quality
> evaluator, OpenAI-compatible endpoint, and `tierkit connect`. See
> [`CHANGELOG.md`](CHANGELOG.md) for the full history.
```

- [ ] **Step 3: Replace the Korean intro block**

Replace the existing `> 🇰🇷 **한글 안내**` block (currently 6 lines starting at the line after the status paragraph, ending at "Install") with:

```markdown
> 🇰🇷 **한글 안내**
>
> Tierkit은 AI 코딩 CLI를 위한 **로컬 우선 비용 최적화 레이어 / 토큰
> 방화벽**입니다. Claude Code, Codex CLI, Gemini CLI, Roo Code, Cline,
> Continue, aider 같은 도구가 비싼 클라우드 모델을 덜 쓰면서도 같은 작업
> 품질을 유지하도록, 클라우드 호출 직전에 로컬에서 파일을 찾고, 압축하고,
> 시크릿을 마스킹하고, baseline vs compressed를 실측 usage로 비교하고,
> 쉬운 작업은 로컬/프라이빗 모델로 처리합니다.
>
> - 빠른 시작: 아래 [Quick start](#quick-start-measure-your-token-savings) 섹션
> - 설치부터 첫 모델 호출: [docs/GUIDE.ko.md](docs/GUIDE.ko.md)
> - VS Code 확장 마켓플레이스 publish: [docs/PUBLISH.ko.md](docs/PUBLISH.ko.md)
> - VS Code 확장 직접 설치: 마켓플레이스 페이지에서 **Install**
```

- [ ] **Step 4: Verify**

Run:
```sh
sed -n '1,30p' README.md
```
Expected: new intro present. Lead sentence is "Tierkit is a **local-first cost optimizer and token firewall for AI coding CLIs**."

Run:
```sh
grep -n "hybrid plugin runtime\|Mission Control" README.md
```
Expected: no matches in the rewritten intro region (lines 1–25). Other regions may still have stale references — they get cleaned in later tasks if they're in the affected sections.

- [ ] **Step 5: Commit**

```sh
git add README.md
git commit -m "docs(v0.11.1): repositioning — README §1 (EN + KR intro)

Lead paragraph + status block + Korean intro all repositioned to
'cost optimizer / token firewall for AI coding CLIs.' Quick start
section replacement comes in the next commit.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

---

## Task 3: `README.md` Quick start replacement + Development setup section

**Files:**
- Modify: `README.md` (the `## Quick start (v0.1)` section — currently somewhere around lines 26 onwards in the original; line numbers shift after Task 2)

- [ ] **Step 1: Locate the current Quick start section**

Run:
```sh
grep -n "^## Quick start\|^## What's in this monorepo" README.md
```
Expected: line number of `## Quick start (v0.1)` and the next H2 heading (likely `## What's in this monorepo` if it exists, otherwise `## Development setup` after this task adds it).

- [ ] **Step 2: Read the current Quick start block to find its end**

Run:
```sh
grep -n "^## " README.md | head -10
```
Expected: list of all H2 headings with line numbers. The current Quick start runs from `## Quick start (v0.1)` to the next H2.

- [ ] **Step 3: Replace the entire Quick start section**

Replace the entire `## Quick start (v0.1)` H2 block (from its heading through to but NOT including the next H2) with:

````markdown
## Quick start: measure your token savings

Requires [ripgrep](https://github.com/BurntSushi/ripgrep) on `PATH` and at
least one model profile with API key (default: `claudeSonnet`).

```sh
# 1. Build a deterministic compressed context for a task — no model call, no cost.
tierkit context build "fix payment success not unlocking premium report"

# 2. (Optional) Inspect what was selected and how it was compressed.
tierkit context show <ctx_id>

# 3. A/B baseline vs compressed on the same task — measures real provider usage.
#    Two paid model calls; TTY confirm or pass --yes for non-interactive.
tierkit context compare <ctx_id> --profile claudeSonnet --yes
```

After `compare` you'll see:

- baseline input tokens (estimated + actual)
- compressed input tokens (estimated + actual)
- savings in tokens, cost, and latency
- both response files saved side-by-side for human quality judgment

That output is the entire product loop. Everything else in Tierkit (cost
routing, plugin policy, workflow sessions, adapter exports) is *how* the
savings get larger and more automatic — see "Cost routing" and "Advanced"
sections of this README.

## Development setup

For working on Tierkit itself:

```sh
pnpm install
pnpm -r build
pnpm -r test --run
```

See [`docs/SPEC.md`](docs/SPEC.md) for the design specification and
[`CHANGELOG.md`](CHANGELOG.md) for release history.
````

- [ ] **Step 4: Verify**

Run:
```sh
grep -n "^## " README.md | head -10
```
Expected: `## Quick start: measure your token savings` and `## Development setup` both present. No `## Quick start (v0.1)` remaining.

Run:
```sh
sed -n '/^## Quick start/,/^## Development setup/p' README.md | head -30
```
Expected: the new Quick start content with `tierkit context build` as the first command shown.

- [ ] **Step 5: Commit**

```sh
git add README.md
git commit -m "docs(v0.11.1): repositioning — README Quick start

Quick start now leads with 'tierkit context build / show / compare' —
the measurement loop is the product. pnpm install / build move to a
new 'Development setup' section.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

---

## Task 4: `docs/SPEC.md` §0–§2 rewrite

**Files:**
- Modify: `docs/SPEC.md` (lines 1 through end-of-§2 — currently §0 Identity, §1 Positioning, §2 Problems Tierkit solves)

- [ ] **Step 1: Find the boundary**

Run:
```sh
grep -n "^## " docs/SPEC.md | head -6
```
Expected: `## 0. Identity`, `## 1. Positioning`, `## 2. Problems Tierkit solves`, `## 3. High-level architecture`. The replacement extends from "## 0. Identity" up to but NOT including "## 3. High-level architecture".

- [ ] **Step 2: Replace §0–§2 (NOT the document header above §0)**

Keep the existing document header (the `# Tierkit — Design Specification` title and the `> Authored by the project owner on 2026-05-15...` blockquote — lines 1–4 of the original).

Replace lines from `## 0. Identity` through the line just before `## 3. High-level architecture` with:

```markdown
## 0. Identity

**Tierkit — Local-first Cost Optimizer for AI Coding CLIs.**

Tierkit sits between an AI coding CLI (Claude Code, Codex CLI, Gemini CLI,
Roo Code, Cline, Continue, aider) and the model providers it would call.
Its job is to spend fewer cloud-model tokens at equal task quality by:

1. compressing repository context with local-first techniques before
   anything leaves the machine;
2. handing as many sub-tasks as possible to local or private-remote models
   (file ranking, summarization, low-risk drafts and reviews);
3. routing the genuinely hard or risky decisions — and only those — to the
   premium cloud model the user actually pays for; and
4. measuring the trade-off every time, so the user can see exactly how
   many tokens they stopped paying for and whether the quality held.

### One-line definition

> Tierkit is a local-first cost optimizer and token firewall for AI coding
> CLIs.

한국어:

> Tierkit은 AI 코딩 CLI가 로컬 모델과 프라이빗 모델을 활용해 클라우드
> 토큰 사용량을 줄이고, 필요한 순간에만 고성능 클라우드 모델로 상승시키는
> 비용 최적화 레이어다.

---

## 1. Positioning

| Aspect | Position |
|---|---|
| What it is | A local-first **cost optimizer and token firewall** for AI coding CLIs. |
| What it is not | A replacement for Claude / GPT / Gemini. Not a coding agent in its own right. Not a plugin marketplace. |
| Primary user outcome | Same coding result, fewer premium-cloud tokens. Credible path from Max 20× → Max 5× → Pro on Claude pricing tiers. |
| Distribution surface | CLI (`tierkit ...`) + VS Code extension + HTTP loopback daemon + OpenAI-compatible gateway + MCP server (from v0.13). |
| Strategic filter | *Does this reduce cloud cost, or stretch the same budget further at equal quality?* If no, it's an advanced/secondary feature. |

The plugin runtime, adapter exports, workflow sessions, and policy rules
remain in the codebase as **implementation mechanisms** of cost
optimization — not as the headline product.

---

## 2. Problems Tierkit solves

1. **Premium-cloud bills compound fast.** Most AI coding sessions burn
   tokens on context that the model didn't actually need to make the right
   decision.
2. **Local compute often sits idle.** Modern dev machines can usefully
   pre-rank files, summarize logs, and compress prompts before any cloud
   call — but no one has tied that to the cloud-CLI flow.
3. **Quality of compression isn't measured.** Most "context shrinkers"
   ship a saved-tokens number with no evidence the model still produces
   the right answer.
4. **Each AI coding CLI re-invents the same policy.** Risk gates, secret
   redaction, budget caps, fallback rules — every tool builds its own
   version, or none.
5. **Private/self-hosted models are second-class.** Existing CLI tools
   treat private endpoints as one of N providers, not as a deliberate
   middle tier between local and public.

Tierkit's answer: one cost layer that sits in front of every coding CLI,
applies the same compression + routing + quality + budget policy, and
shows the user — every day — how much they saved and whether the work
held up.

---

```

(The trailing `---` and the `## 3. High-level architecture` heading that follows stay as-is.)

- [ ] **Step 3: Verify**

Run:
```sh
sed -n '/^## 0. Identity/,/^## 3. /p' docs/SPEC.md | head -20
```
Expected: new §0 content with "Local-first Cost Optimizer for AI Coding CLIs" subtitle.

Run:
```sh
grep -n "plugin OS\|Local-first Hybrid Coding Agent" docs/SPEC.md
```
Expected: no matches.

- [ ] **Step 4: Commit**

```sh
git add docs/SPEC.md
git commit -m "docs(v0.11.1): repositioning — SPEC §0–§2

Identity, Positioning, and Problems sections all anchored to
cost-optimizer framing. Plugin/adapter/session content stays in the
codebase but moves to 'implementation mechanism' role.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

---

## Task 5: `docs/INTEGRATIONS.md` lead rewrite

**Files:**
- Modify: `docs/INTEGRATIONS.md` (title + first 6 lines)

- [ ] **Step 1: Check current state**

Run:
```sh
sed -n '1,10p' docs/INTEGRATIONS.md
```
Expected: starts with `# Tierkit — Tool integrations (v1.3 / v1.4)` then a brief intro.

- [ ] **Step 2: Find the boundary**

Run:
```sh
grep -n "^## " docs/INTEGRATIONS.md | head -5
```
Expected: list of H2 headings. The replacement extends from line 1 through the line just before the first H2.

- [ ] **Step 3: Replace the title + intro paragraph**

Replace lines 1 through the line just before the first H2 with:

```markdown
# Tierkit — Integrations as savings channels

Every supported AI coding CLI is a place Tierkit can save tokens. Each
integration below is rated by its *savings posture* (how aggressively
Tierkit can compress + route on that tool's behalf) and its *interception
style* (gateway / helper / MCP / export-first). Integrations are not
themselves a milestone — they are how the cost layer reaches the user.

## Interception styles

- **Gateway** — Tierkit's OpenAI-compatible endpoint is the tool's model
  provider. Compression and routing happen server-side before the request
  leaves the machine. Used by: Codex CLI, Roo Code, Cline, Continue, aider.
- **Helper** — Tierkit exposes commands (`tierkit context build/send`)
  that the user invokes alongside the tool. The tool's own agent loop is
  not intercepted. Used by: Claude Code today (until MCP path is validated).
- **MCP** — Tierkit exposes itself as an MCP server the tool can call
  when it *wants* compressed context. Planned: Claude Code, Gemini CLI
  (v0.13+).
- **Export-first** — Tierkit writes policy files (`.roomodes`,
  `.clinerules/`, `.continue/config.yaml`) that the tool reads at startup.
  Lighter-touch integration, no live cost layer. Useful as a fallback.

## Savings posture per integration

| Tool | Style today | Style target | Savings effective |
|---|---|---|---|
| Roo Code             | gateway (`tierkit connect roo`)    | gateway + compression policy (v0.13) | partial |
| Cline                | gateway (`tierkit connect cline`)  | gateway + compression policy (v0.13) | partial |
| Continue             | gateway (`tierkit connect continue`)| gateway + compression policy (v0.13) | partial |
| Codex CLI            | gateway (manual `base_url`)        | first-class `connect` + compression policy (v0.13) | partial, manual |
| aider                | gateway (manual `--openai-api-base`)| first-class `connect` + compression policy (v0.13) | partial, manual |
| Claude Code          | helper (`tierkit context build/send`) | helper + MCP server (v0.13)        | manual today |
| Gemini CLI           | (none)                             | MCP / compressed-context provider (v0.13) | not yet |
```

The rest of the file (existing per-tool wiring instructions, OpenAI-compatible endpoint URL, etc.) stays unchanged.

- [ ] **Step 4: Verify**

Run:
```sh
sed -n '1,30p' docs/INTEGRATIONS.md
```
Expected: new title "Tierkit — Integrations as savings channels," interception-styles section, and the savings-posture table with Roo/Cline/Continue at top and Codex/aider marked as manual.

- [ ] **Step 5: Commit**

```sh
git add docs/INTEGRATIONS.md
git commit -m "docs(v0.11.1): repositioning — INTEGRATIONS as savings channels

Reframes integrations as the channels through which the cost layer
reaches the user. Adds an interception-style taxonomy (gateway /
helper / MCP / export-first) and a per-tool savings-posture table.
Accurately records that 'tierkit connect' supports roo/cline/continue
only; Codex CLI and aider are manual gateway config today.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

---

## Task 6: CLI command `category:` field reorganization

**Files:**
- Modify: every CLI command's `static override usage = Command.Usage({ category: ... })` block. The full set:
  - `packages/cli/src/commands/InitCommand.ts`
  - `packages/cli/src/commands/DoctorCommand.ts`
  - `packages/cli/src/commands/ConnectCommand.ts`
  - `packages/cli/src/commands/UsageCommand.ts`
  - `packages/cli/src/commands/check/CheckCommandCommand.ts`
  - `packages/cli/src/commands/check/CheckPathCommand.ts`
  - `packages/cli/src/commands/check/CheckRedactCommand.ts`
  - `packages/cli/src/commands/config/ConfigShowCommand.ts`
  - `packages/cli/src/commands/context/ContextBuildCommand.ts`
  - `packages/cli/src/commands/context/ContextShowCommand.ts`
  - `packages/cli/src/commands/context/ContextSendCommand.ts`
  - `packages/cli/src/commands/context/ContextCompareCommand.ts`
  - `packages/cli/src/commands/export/ExportGenericCommand.ts`
  - `packages/cli/src/commands/export/ExportRooCommand.ts`
  - `packages/cli/src/commands/export/ExportClineCommand.ts`
  - `packages/cli/src/commands/export/ExportContinueCommand.ts`
  - `packages/cli/src/commands/models/ModelsListCommand.ts`
  - `packages/cli/src/commands/models/ModelsTestCommand.ts`
  - `packages/cli/src/commands/plugin/PluginValidateCommand.ts`
  - `packages/cli/src/commands/plugin/PluginInstallCommand.ts`
  - `packages/cli/src/commands/plugin/PluginListCommand.ts`
  - `packages/cli/src/commands/plugin/PluginEnableCommand.ts`
  - `packages/cli/src/commands/plugin/PluginDisableCommand.ts`
  - `packages/cli/src/commands/plugin/PluginRemoveCommand.ts`
  - `packages/cli/src/commands/plugin/PluginSyncCommand.ts`
  - `packages/cli/src/commands/plugin/PluginNewCommand.ts`
  - `packages/cli/src/commands/profile/ProfileAddCommand.ts`
  - `packages/cli/src/commands/profile/ProfileRemoveCommand.ts`
  - `packages/cli/src/commands/route/RouteExplainCommand.ts`
  - `packages/cli/src/commands/route/RouteRunCommand.ts`
  - `packages/cli/src/commands/runtime/RuntimeStartCommand.ts`
  - `packages/cli/src/commands/runtime/RuntimeStopCommand.ts`
  - `packages/cli/src/commands/runtime/RuntimeStatusCommand.ts`
  - `packages/cli/src/commands/session/SessionStartCommand.ts`
  - `packages/cli/src/commands/session/SessionStatusCommand.ts`
  - `packages/cli/src/commands/session/SessionAdvanceCommand.ts`
  - `packages/cli/src/commands/session/SessionApprovePlanCommand.ts`
  - `packages/cli/src/commands/session/SessionAbandonCommand.ts`

**Mapping (old `category:` → new `category:`):**

| Command(s) | Old category | New category |
|---|---|---|
| `ContextBuildCommand`, `ContextShowCommand`, `ContextSendCommand`, `ContextCompareCommand`, `UsageCommand` | `Context` / `Usage` | `Savings` |
| `RouteExplainCommand`, `RouteRunCommand` | `Routing` | `Cost routing` |
| `ModelsListCommand`, `ModelsTestCommand` | `Models` | `Cost routing` |
| `ConfigShowCommand` | `Config` | `Cost routing` |
| `ConnectCommand` | `Connect` | `Integrations` |
| `ExportGenericCommand`, `ExportRooCommand`, `ExportClineCommand`, `ExportContinueCommand` | `Export` | `Integrations` |
| `DoctorCommand` | `Doctor` | `Diagnostics` |
| `CheckCommandCommand`, `CheckPathCommand`, `CheckRedactCommand` | `Check` | `Diagnostics` |
| `RuntimeStartCommand`, `RuntimeStopCommand`, `RuntimeStatusCommand` | `Runtime` | `Diagnostics` |
| All `PluginXxxCommand` (8 commands) | `Plugin` | `Advanced` |
| All `SessionXxxCommand` (5 commands) | `Session` | `Advanced` |
| `ProfileAddCommand`, `ProfileRemoveCommand` | `Profile` | `Advanced` |
| `InitCommand` | `Init` | `Advanced` |

- [ ] **Step 1: Confirm one example before bulk editing**

Run:
```sh
grep -A2 "static override usage" packages/cli/src/commands/context/ContextBuildCommand.ts | head -5
```
Expected: `static override usage = Command.Usage({ category: "Context", ...` (the current value).

- [ ] **Step 2: Apply the category rewrites file by file**

For each file in the list above, edit the `category: "<old>"` value to `category: "<new>"` per the mapping. Use the Edit tool with `old_string` and `new_string` set to the matching pair, scoped tight enough that the change is unambiguous within each file.

Example for `ContextBuildCommand.ts`:

```ts
// Before:
  static override usage = Command.Usage({
    category: "Context",
// After:
  static override usage = Command.Usage({
    category: "Savings",
```

Repeat for every file using the table above.

- [ ] **Step 3: Update `ContextBuildCommand`'s description to lead with savings (per §13 acceptance)**

In `packages/cli/src/commands/context/ContextBuildCommand.ts`, change the description from the existing copy to:

```ts
    description: "Find candidate files locally and build a compressed prompt (no LLM, no cost). Run `context compare` to measure savings.",
```

Make similar small adjustments to `ContextShowCommand`, `ContextSendCommand`, `ContextCompareCommand` descriptions if they don't already lead with savings framing — check current text first with `grep description packages/cli/src/commands/context/*.ts`.

- [ ] **Step 4: Run typecheck and tests**

Run:
```sh
pnpm --filter @tierkit/cli typecheck
```
Expected: clean.

Run:
```sh
pnpm --filter @tierkit/cli test -- --run
```
Expected: 40/40 passing (no test asserts category strings).

- [ ] **Step 5: Build CLI and visually inspect `--help`**

Run:
```sh
pnpm --filter @tierkit/cli build
node packages/cli/dist/index.js --help | head -80
```
Expected: top-level help now groups commands under `Savings`, `Cost routing`, `Integrations`, `Diagnostics`, `Advanced`. `tierkit context build` is in `Savings`. `tierkit route explain` is in `Cost routing`. `tierkit plugin list` is in `Advanced`.

- [ ] **Step 6: Commit**

```sh
git add packages/cli/src/commands/
git commit -m "feat(cli, v0.11.1): repositioning — top-level help categories

Reorganizes the 38 CLI commands under 5 cost-framed categories:
Savings (context build/show/send/compare + usage), Cost routing
(route, models, config), Integrations (connect, export), Diagnostics
(doctor, check, runtime), Advanced (plugin, session, profile, init).
String-only edits to existing Command.Usage blocks. No command path,
flag, or behavior changes.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

---

## Task 7: `packages/core/src/runtime/ui/gui.ts` — Mission Control → Cost Control, i18n + grouping

**Files:**
- Modify: `packages/core/src/runtime/ui/gui.ts`

**Constraints (per spec §13 and §10):**
- No new fetches, no new endpoints, no filesystem reads.
- `cardCurrentArtifact` is a static stub with the text `"Last compressed context: — · Run \`tierkit context build\` to create one."`
- `cardSavingsToday` reads only from the existing `/v1/usage` endpoint; metrics not derivable from that endpoint render as `—`.
- Existing cards (`cardTools`, `cardPlugins`, `cardModels`, `cardDaemon`, `cardUsage`) keep their behavior; only their group, order, and visible heading text change.

- [ ] **Step 1: Update the header comment**

Find `packages/core/src/runtime/ui/gui.ts:2`:
```
 * Single-file browser UI for the Tierkit runtime daemon — Mission Control.
```
Change to:
```
 * Single-file browser UI for the Tierkit runtime daemon — Cost Control.
```

- [ ] **Step 2: Replace existing i18n group labels and add new keys**

Find the English i18n table in `gui.ts` (look for `groupIntegrations: 'Integrations'`, around line 730 or so). The current English block contains keys like:
- `groupIntegrations: 'Integrations'`
- `groupConfiguration: 'Configuration'`
- `groupActivity: 'Activity'`
- `cardTools: 'Connected tools'`
- `cardPlugins: 'Active plugins'`
- `cardActivity: 'Recent activity'`
- `cardUsage: 'Today's usage'`
- `cardModels: 'Model profiles'`
- `cardDaemon: 'Daemon'`

Replace **only the group-level keys** with cost-framed ones and **add new keys**. Final English keys (additive — do not remove any existing key the GUI body still references):

```ts
// inside the English i18n object — replace the existing 3 group keys
groupSavings: 'Savings',
groupCurrentProject: 'Current project',
groupCostRouting: 'Cost routing',
groupIntegrations: 'Integrations',          // existing key, label updated below if it differs
groupAdvanced: 'Advanced',

// new card-level keys (additive)
cardSavingsToday: 'Today',
cardCurrentArtifact: 'Last compressed context',
cardLocalModels: 'Local + private + cloud models',
labelLocalCompressor: 'Local Compressor',
labelLocalReviewer: 'Local Reviewer',
labelCloudFinal: 'Cloud Final',
labelTierOrder: 'Tier order',
labelTierOrderValue: 'local → private → public',
actionBuild: 'Build compressed context',
actionCompare: 'Compare baseline vs compressed',
actionSend: 'Send compressed prompt',
hintBuildContext: 'Run `tierkit context build` to create one.',
hintNoMeasurements: 'Run `tierkit context compare` to start measuring savings.',

// existing card labels that move under Advanced — keep their keys, update visible labels
cardPlugins: 'Active policy rules',      // was "Active plugins"
cardModels: 'Model profile editor',       // was "Model profiles"
cardDaemon: 'Daemon controls',            // was "Daemon"
cardUsage: 'Raw usage log',               // was "Today's usage"
```

The Korean i18n object lives just below the English one. Apply the same key set:

```ts
// inside the Korean i18n object — replace existing 3 group keys
groupSavings: '절감',
groupCurrentProject: '현재 프로젝트',
groupCostRouting: '비용 라우팅',
groupIntegrations: '연결된 도구',
groupAdvanced: '고급',

// new card-level keys (additive)
cardSavingsToday: '오늘',
cardCurrentArtifact: '마지막 압축 컨텍스트',
cardLocalModels: '로컬 + 프라이빗 + 클라우드 모델',
labelLocalCompressor: '로컬 압축기',
labelLocalReviewer: '로컬 리뷰어',
labelCloudFinal: '클라우드 최종',
labelTierOrder: '계층 순서',
labelTierOrderValue: '로컬 → 프라이빗 → 퍼블릭',
actionBuild: '압축 컨텍스트 생성',
actionCompare: '베이스라인 vs 압축 비교',
actionSend: '압축 프롬프트 전송',
hintBuildContext: '`tierkit context build` 를 실행해 생성하세요.',
hintNoMeasurements: '`tierkit context compare` 를 실행하면 절감 측정이 시작됩니다.',

cardPlugins: '활성 정책 규칙',
cardModels: '모델 프로파일 편집',
cardDaemon: '데몬 제어',
cardUsage: '원본 사용량 로그',
```

The old keys `groupConfiguration` and `groupActivity` are removed (replaced by the cost-framed groups above). Search the file body for any usage of these two keys with `grep -n "groupConfiguration\|groupActivity" packages/core/src/runtime/ui/gui.ts` and update each call site to the new group names listed below.

- [ ] **Step 3: Update card grouping in the HTML template**

Find the existing card grouping markers in `gui.ts` (around lines 622–717):
```html
<h3 class="settings-group" data-i18n="groupIntegrations">Integrations</h3>
<section class="card">...cardTools...</section>
<section class="card">...cardPlugins...</section>

<h3 class="settings-group" data-i18n="groupConfiguration">Configuration</h3>
<section class="card">...cardModels...</section>
<section class="card">...cardDaemon...</section>

<h3 class="settings-group" data-i18n="groupActivity">Activity</h3>
<section class="card full">...cardUsage...</section>
```

Reorganize as:

```html
<h3 class="settings-group" data-i18n="groupSavings">Savings</h3>
<section class="card">
  <h2><span data-i18n="cardSavingsToday">Today</span></h2>
  <!-- Existing usage-summary refresh logic renders here. Cells render as '—' when /v1/usage has no data. -->
  <div id="card-savings-today-body">
    <div class="metric"><span data-i18n="metricTokensSaved">Cloud input tokens saved</span>: <span id="m-tokens-saved">—</span></div>
    <div class="metric"><span data-i18n="metricCostSaved">Estimated cost saved</span>: <span id="m-cost-saved">—</span></div>
    <div class="metric"><span data-i18n="metricCallsAvoided">Cloud calls avoided</span>: <span id="m-calls-avoided">—</span></div>
    <div class="dim" data-i18n="hintNoMeasurements">Run `tierkit context compare` to start measuring savings.</div>
  </div>
</section>

<h3 class="settings-group" data-i18n="groupCurrentProject">Current project</h3>
<section class="card">
  <h2><span data-i18n="cardCurrentArtifact">Last compressed context</span></h2>
  <!-- v0.11.1: static stub. NO filesystem scan. v0.12 will populate from real state. -->
  <div class="metric">— · <span data-i18n="hintBuildContext">Run `tierkit context build` to create one.</span></div>
</section>

<h3 class="settings-group" data-i18n="groupCostRouting">Cost routing</h3>
<section class="card">
  <h2><span data-i18n="cardLocalModels">Local + private + cloud models</span></h2>
  <!-- Reuses the existing models-card body. Role labels are read-only in v0.11.1. -->
  <div class="card-divider"><div class="card-divider-label" data-i18n="labelLocalCompressor">Local Compressor</div></div>
  <!-- existing Models card content begins -->
  ...EXISTING MODELS CARD BODY (the <table>, the test buttons, the API-keys section, the presets row, the threshold disclosure) goes here verbatim — DO NOT delete or modify the existing markup; only relocate it under this new heading...
  <!-- existing Models card content ends -->
  <div class="card-divider"><div class="card-divider-label" data-i18n="labelTierOrder">Tier order</div></div>
  <div class="dim" data-i18n="labelTierOrderValue">local → private → public</div>
</section>

<h3 class="settings-group" data-i18n="groupIntegrations">Integrations</h3>
<section class="card">
  <!-- existing cardTools content unchanged -->
  ...EXISTING TOOLS CARD BODY verbatim...
</section>

<h3 class="settings-group" data-i18n="groupAdvanced">Advanced</h3>
<details class="advanced-collapse">
  <summary>Show advanced cards</summary>
  <section class="card">
    <!-- existing Plugins card; relabel only -->
    <h2><span data-i18n="cardPlugins">Active policy rules</span></h2>
    ...EXISTING PLUGINS CARD BODY verbatim...
  </section>
  <section class="card">
    <h2><span data-i18n="cardDaemon">Daemon controls</span></h2>
    ...EXISTING DAEMON CARD BODY verbatim...
  </section>
  <section class="card full">
    <h2><span data-i18n="cardUsage">Raw usage log</span></h2>
    ...EXISTING USAGE CARD BODY verbatim...
  </section>
</details>
```

**Important rules for this step:**

- **Do not delete any existing card body.** Move them under the new group headings. The internal markup (table rows, buttons, refresh logic) stays identical.
- The `cardModels` body moves under `groupCostRouting`. It keeps the same id targets so refresh functions don't break.
- The `cardUsage` card stays alive but moves under Advanced (the new `cardSavingsToday` is a *summary* view that derives from the same data; the raw usage table remains accessible).
- The `cardSavingsToday` body uses three new DOM ids (`m-tokens-saved`, `m-cost-saved`, `m-calls-avoided`). For v0.11.1, leave them with `—` placeholder text. **Do not** add new fetch logic — the existing `refreshUsage()` function can populate them if the daemon's `/v1/usage` response already has the relevant fields; if not, leave them as `—`.

- [ ] **Step 4: Inspect refresh-function wiring**

Run:
```sh
grep -nE "refreshUsage|refreshPlugins|refreshModels|refreshTools" packages/core/src/runtime/ui/gui.ts | head -20
```

For each refresh function: confirm the DOM ids it targets still exist after the reorganization (they should, since you moved markup without renaming ids).

If `refreshUsage()` currently populates `cardUsage`'s table, leave that wiring alone — the table is now under Advanced. **Do not** add new code that populates `m-tokens-saved` / `m-cost-saved` / `m-calls-avoided` if doing so would require a new endpoint call. If the existing `/v1/usage` response already includes a per-day summary, you may populate them inside `refreshUsage()` with a 3-line addition; otherwise leave them as `—`.

- [ ] **Step 5: Update the in-test comment that says "Mission Control UI" (optional, for consistency)**

In `packages/core/test/runtimeServerSessionAndUi.test.ts:153`, change:
```ts
    // The Mission Control UI hits these endpoints (no chat input — Tierkit is a policy
```
to:
```ts
    // The Cost Control UI hits these endpoints (no chat input — Tierkit is a policy
```
This is a comment-only change with no behavioral impact.

- [ ] **Step 6: Run typecheck and full test suite**

Run:
```sh
pnpm --filter @tierkit/core typecheck
```
Expected: clean.

Run:
```sh
pnpm --filter @tierkit/core build && pnpm -r test -- --run
```
Expected: 635/635 passing across all packages. No regression.

- [ ] **Step 7: Smoke-test the daemon**

Run:
```sh
pnpm --filter @tierkit/core build
node -e "import('./packages/core/dist/index.js').then(m => process.stdout.write(m.GUI_HTML.slice(0, 200)))"
```
Expected: the HTML output starts with the doctype + head and the file no longer contains "Mission Control" anywhere.

Run:
```sh
node -e "import('./packages/core/dist/index.js').then(m => { if (m.GUI_HTML.includes('Mission Control')) { console.error('STALE STRING'); process.exit(1); } })"
```
Expected: no output (silent success).

Run:
```sh
node -e "import('./packages/core/dist/index.js').then(m => { for (const s of ['groupSavings','groupCostRouting','groupAdvanced','cardSavingsToday','cardCurrentArtifact','labelLocalCompressor']) { if (!m.GUI_HTML.includes(s)) { console.error('MISSING:',s); process.exit(1); } } })"
```
Expected: silent success.

- [ ] **Step 8: Commit**

```sh
git add packages/core/src/runtime/ui/gui.ts packages/core/test/runtimeServerSessionAndUi.test.ts
git commit -m "feat(ui, v0.11.1): repositioning — Cost Control sidebar

Renames Mission Control to Cost Control. Reorders sidebar cards into
Savings (today's tokens / cost / calls saved — stubs render '—' until
real data), Current project (last-compressed-context placeholder, no
filesystem scan in v0.11.1), Cost routing (existing models card under
clearer heading), Integrations (unchanged Tools), Advanced (Plugins
as 'policy rules', Daemon, raw Usage). Korean and English i18n keys
updated in lockstep. No new fetches, no new endpoints, no filesystem
reads. All card bodies preserved verbatim — only headings, grouping,
and order changed.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

---

## Task 8: `CHANGELOG.md` 0.11.1 entry

**Files:**
- Modify: `CHANGELOG.md` (prepend a new section above the existing `## 0.11.0 — 2026-05-19` entry)

- [ ] **Step 1: Read current state**

Run:
```sh
head -5 CHANGELOG.md
```
Expected: `# Changelog` then blank then `## 0.11.0 — 2026-05-19`.

- [ ] **Step 2: Insert the 0.11.1 entry above 0.11.0**

Edit `CHANGELOG.md` so the top of the file reads:

```markdown
# Changelog

## 0.11.1 — 2026-05-19

### Changed (docs / IA realignment — zero runtime change)

- **Repositioning.** Tierkit is now described, throughout README / SPEC /
  INTEGRATIONS / VS Code marketplace listing / GUI sidebar / CLI
  top-level help / all 9 workspace `package.json` descriptions, as a
  **local-first cost optimizer and token firewall for AI coding CLIs**
  (Claude Code, Codex CLI, Gemini CLI, Roo Code, Cline, Continue, aider).
  The previous "local-first hybrid plugin runtime" framing is replaced.
  Plugin runtime, adapters, sessions, and model profiles remain in the
  codebase as implementation mechanisms; they move to the Advanced
  section in docs/UI rather than being removed.
- **README Quick start** now leads with `tierkit context build` /
  `tierkit context compare`. `pnpm install` moves to a new
  "Development setup" section.
- **GUI sidebar** renamed from "Mission Control" to "Cost Control"; cards
  reorganized into Savings / Current project / Cost routing /
  Integrations / Advanced.
- **CLI top-level help** now groups commands under Savings / Cost routing
  / Integrations / Diagnostics / Advanced (string-only edits to the
  `Command.Usage({ category: ... })` fields).
- **docs/INTEGRATIONS.md** reframed as "Integrations as savings channels"
  with an interception-style taxonomy (gateway / helper / MCP /
  export-first) and a per-tool savings-posture table. Accurately records
  that `tierkit connect` supports `roo | cline | continue` only — Codex
  CLI and aider are listed as manual gateway-config today, with
  first-class `connect` deferred to v0.13.

### Not changed in 0.11.1 (deliberate)

- No CLI command path, flag, or behavior changes.
- No HTTP endpoint added / removed / renamed.
- No `ModelProfile` / `tierkit.config.json` schema changes.
- No new filesystem reads or writes (the "Current project" card is an
  inert stub — first-class "last artifact" tracking is a v0.12 feature).
- No new tests; existing 635/635 test count and `pnpm -r typecheck`
  remain green.

### Strategic filter going forward

From v0.11.1 onward, every feature is evaluated against the question:
**"Does this reduce cloud cost, or stretch the same budget further at
equal quality?"** If the answer is no, it's an advanced/secondary
feature, not a headline.

## 0.11.0 — 2026-05-19
```

(Everything below the inserted block stays exactly as it was.)

- [ ] **Step 3: Verify**

Run:
```sh
head -50 CHANGELOG.md
```
Expected: `## 0.11.1 — 2026-05-19` at the top under `# Changelog`, followed by the new content, then `## 0.11.0 — 2026-05-19` and the existing entry.

- [ ] **Step 4: Commit**

```sh
git add CHANGELOG.md
git commit -m "chore(release): 0.11.1 — repositioning entry in CHANGELOG

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

---

## Task 9: Memory note + MEMORY.md index update

**Files (outside the repo):**
- Create: `/Users/siwal/.claude/projects/-Users-siwal-code-Tierkit/memory/project_v17_1_repositioning.md`
- Modify: `/Users/siwal/.claude/projects/-Users-siwal-code-Tierkit/memory/MEMORY.md`

These files live in the user's auto-memory directory, not in the git repo. They persist across sessions for future Claude Code instances.

- [ ] **Step 1: Create the memory file**

Write `/Users/siwal/.claude/projects/-Users-siwal-code-Tierkit/memory/project_v17_1_repositioning.md`:

```markdown
---
name: project-v17-1-repositioning
description: Tierkit v0.11.1 — repositioning from "local-first hybrid plugin runtime" to "local-first cost optimizer and token firewall for AI coding CLIs." Docs/copy/UI-IA only; zero runtime change. Released 2026-05-19.
metadata:
  type: project
---

v0.11.1 is a docs / copy / UI-IA realignment. **No runtime code, no schema, no endpoint, no CLI command behavior changed.** The point is to fix a coherence problem: the project had grown features faster than its product story, and the README / SPEC / GUI / CLI help / package descriptions still led with "plugin runtime" while the actual value prop had become "spend fewer cloud tokens at equal coding quality."

**Why:** every design decision from v0.12 onward should pass a single filter — *"does this reduce cloud cost, or stretch the same budget further at equal quality?"* If the answer is no, it's an advanced/secondary feature. That filter only works if the lead surfaces tell the same story.

**How to apply:** when evaluating new feature proposals, check them against the cost-optimization filter. If a proposal is "let's add another plugin format / another adapter / another workflow mode" and doesn't reduce cloud cost, it's a v0.13+ Advanced consideration, not v0.12 headline.

## What changed in v0.11.1

- README §1 (English + Korean intro) → cost-optimizer framing.
- README Quick start → leads with `tierkit context build` / `compare`; `pnpm install` moves to "Development setup".
- docs/SPEC.md §0–§2 → identity / positioning / problems all repositioned.
- docs/INTEGRATIONS.md → "Integrations as savings channels" with interception-style taxonomy + savings-posture table per tool.
- Root + 9 workspace `package.json` descriptions → cost-optimizer wording.
- `packages/vscode-tierkit/package.nls.json` → marketplace listing updated.
- `packages/core/src/runtime/ui/gui.ts` → header comment "Mission Control" → "Cost Control"; sidebar groups become Savings / Current project / Cost routing / Integrations / Advanced; new i18n keys (EN + KR) added in lockstep; old `groupConfiguration` and `groupActivity` removed.
- CLI top-level `--help` → 5 cost-framed categories: Savings, Cost routing, Integrations, Diagnostics, Advanced. All commands recategorized via `Command.Usage({ category: ... })` edits.
- `CHANGELOG.md` → `## 0.11.1 — 2026-05-19` entry added.

## What stayed the same (deliberate)

- All CLI commands keep paths, flags, exit codes, behavior.
- All HTTP endpoints unchanged.
- `tierkit.config.json` schema unchanged.
- `ModelProfile` shape unchanged.
- Plugin loader / registry / adapter exports unchanged.
- Sessions / superpowers rule packs / generated plugins all still work; just demoted to Advanced in docs/UI.
- 635/635 tests stay green. `pnpm -r typecheck` stays green.

## What deliberately did NOT happen in v0.11.1

- **No filesystem scan** for the new "Current project" card. The card is a static "Run `tierkit context build` to create one" stub. First-class last-artifact tracking is a v0.12 deliverable.
- **No fake savings numbers.** The new Savings card renders `—` for any metric not derivable from the existing `/v1/usage` data. No vaporware-feel "$2.53 saved" placeholders.
- **No `roles[]` field on `ModelProfile`.** The per-role local-model selector in the spec mockup is a v0.12 deliverable; v0.11.1's "Local Compressor / Local Reviewer" labels are visual only.
- **No `tierkit connect codex` / `tierkit connect aider`.** Verified: `tierkit connect` supports roo/cline/continue only. Codex and aider are documented as manual gateway config until v0.13.
- **No MCP server.** Deferred to v0.13.
- **No rename of `superpowers-*` plugin slugs or `@tierkit/plugin-superpowers` package.** Deferred to v0.13 with migration shim.

## Roadmap anchor (v0.11.1 frames the next milestones)

- **0.12** — local LLM file-ranker / context-compressor / log-summarizer / diff-summarizer; per-role model assignment lands on `ModelProfile`; device-tier presets (32 / 64 / 128 GB).
- **0.13** — CLI Gateway: compression policy on the OpenAI-compatible endpoint (auto-apply to Codex / Roo / Cline / Continue / aider); MCP server for Claude Code; MCP provider for Gemini CLI. Plugin-slug renames land here.
- **0.14** — Cost Router formalization: cheapest-acceptable-path semantics, private-remote first-class.
- **0.15** — Savings Intelligence: weekly report, Max-tier downgrade estimators.

See [[project-v17-context-compression]] for the v1.7-spike measurement foundation that this repositioning depends on; [[project-tierkit-identity]] for the underlying design principles (loopback-only daemon, no telemetry, additive surface).
```

- [ ] **Step 2: Update `MEMORY.md` index**

Open `/Users/siwal/.claude/projects/-Users-siwal-code-Tierkit/memory/MEMORY.md` and add this line at the end of the project list, after the existing v1.7-spike entry:

```markdown
- [Project: v0.11.1 repositioning](project_v17_1_repositioning.md) — 2026-05-19; docs/copy/UI-IA realignment only (zero runtime change). Tierkit reframed as a local-first cost optimizer / token firewall for AI coding CLIs; plugin runtime stays in code, demoted to Advanced. Strategic filter for v0.12+ recorded.
```

(Insert it directly after the `project_v17_context_compression.md` line.)

- [ ] **Step 3: Verify**

Run:
```sh
ls -la /Users/siwal/.claude/projects/-Users-siwal-code-Tierkit/memory/project_v17_1_repositioning.md
grep "project_v17_1_repositioning" /Users/siwal/.claude/projects/-Users-siwal-code-Tierkit/memory/MEMORY.md
```
Expected: file exists with non-zero size; the grep finds the new index line.

(No commit — memory files live outside the repo.)

---

## Task 10: Final verification

**Files:** none modified in this task.

- [ ] **Step 1: Run the full test suite from scratch**

Run:
```sh
cd /Users/siwal/code/Tierkit
pnpm -r build
pnpm -r test -- --run
pnpm -r typecheck
```
Expected:
- `pnpm -r build`: all 9 buildable packages succeed.
- `pnpm -r test -- --run`: total tests across all packages = **635** (unchanged from before the repositioning PR). All pass.
- `pnpm -r typecheck`: clean across all 9 workspaces.

- [ ] **Step 2: Grep for any remaining old-positioning strings**

Run:
```sh
grep -rnE "hybrid plugin runtime|Local-first hybrid|Mission Control" \
  README.md docs/ packages/*/package.json packages/vscode-tierkit/package.nls.json \
  packages/core/src/runtime/ui/gui.ts 2>/dev/null
```

Expected: **no matches** — these strings should be entirely gone from the user-facing surfaces. (Matches inside `node_modules/`, `dist/`, or `.git/` would be fine but shouldn't appear here since we restricted the grep.)

If any match shows up, fix it before continuing.

- [ ] **Step 3: Grep that the new lead identity is present**

Run:
```sh
grep -lE "cost optimizer|token firewall|Cost Control" \
  README.md docs/SPEC.md docs/INTEGRATIONS.md \
  packages/core/src/runtime/ui/gui.ts CHANGELOG.md
```
Expected: at least these files match — README.md, docs/SPEC.md, docs/INTEGRATIONS.md, packages/core/src/runtime/ui/gui.ts, CHANGELOG.md.

- [ ] **Step 4: Visually inspect the new CLI help**

Run:
```sh
node packages/cli/dist/index.js --help | head -60
```
Expected: top-level `--help` lists 5 categories in order — `Savings`, `Cost routing`, `Integrations`, `Diagnostics`, `Advanced`. `tierkit context build` appears under `Savings`.

- [ ] **Step 5: Visually inspect the new GUI**

Run:
```sh
node packages/cli/dist/index.js runtime start
```
(or however the daemon is normally launched).

Open `http://127.0.0.1:4101/` in a browser. Expected:
- Page title / banner says "Cost Control" (or similar — no "Mission Control").
- First card group is "Savings" with stub `—` placeholders.
- "Current project" card shows "Last compressed context: — · Run `tierkit context build` to create one."
- "Cost routing" group contains the (relocated) Models card with presets + threshold disclosure intact.
- Switching language to 한국어 shows the Korean strings (절감 / 현재 프로젝트 / 비용 라우팅 / ...).
- The Advanced details collapse reveals Active policy rules / Daemon controls / Raw usage log.

Stop the daemon when done.

- [ ] **Step 6: Final commit if needed**

If steps 1–5 surfaced any small fixes (e.g., a leftover "Mission Control" comment, a typo), apply them and commit:

```sh
git add <whatever-was-fixed>
git commit -m "chore(v0.11.1): post-verification touch-ups

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>"
```

If nothing needs fixing, skip this step.

---

## Spec coverage self-check

Every spec acceptance criterion in §13 maps to a task:

| §13 criterion | Task |
|---|---|
| README §1 matches Appendix A (English) verbatim | Task 2 |
| README §1 (Korean) matches Appendix B verbatim | Task 2 |
| README "Quick start" matches Appendix H verbatim | Task 3 |
| docs/SPEC.md §0–§2 matches Appendix C verbatim | Task 4 |
| docs/INTEGRATIONS.md lead matches Appendix G verbatim | Task 5 |
| Root `package.json` description matches new wording | Task 1 |
| 9 workspace package descriptions match Appendix F | Task 1 |
| `package.nls.json` description matches new wording | Task 1 |
| `gui.ts:2` "Mission Control" → "Cost Control" | Task 7 |
| GUI card groups renamed and reordered per §10 + Appendix D | Task 7 |
| i18n keys updated; new keys added | Task 7 |
| `tierkit --help` categories reorganized per Appendix E | Task 6 |
| context build/show/send/compare descriptions reflect cost framing | Task 6 |
| Tests stay 635/635, typecheck clean | Task 10 |
| No source file change except `gui.ts` and CLI `usage` blocks | Tasks 6, 7 (the only `packages/*/src/` files modified) |
| No new HTTP endpoint / CLI command / schema field | Verified by Task 10 grep + test |
| No new filesystem reads/writes | Task 7 explicit constraint; verified by Task 10 |
| CHANGELOG `0.11.1` entry | Task 8 |
| Memory note + cost-filter principle recorded | Task 9 |
