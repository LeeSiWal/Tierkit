node_modules
dist
.turbo
.cache
*.log
.DS_Store
.env
.env.*
!.env.example
coverage
*.tsbuildinfo
*.vsix

# Local Tierkit runtime data (usage logs, PID files, etc.) — generated when running
# the daemon from this directory for testing
.tierkit/runtime/

# Claude Code worktrees and harness artifacts
.claude/
